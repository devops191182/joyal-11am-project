---
- name: Create a Pod in Kubernetes
  hosts: all
  gather_facts: no
  collections:
    - community.kubernetes

  tasks:
    - name: Create a simple nginx pod
      k8s:
        state: present
        definition:
          apiVersion: v1
          kind: Pod
          metadata:
            name: croma-pod
            namespace: default
          spec:
            containers:
              - name: cont-01
                image: ubuntu:24.04
                command: ["sleep", "3600"]
              - name: cont-02
                image: ubuntu:22.04
                command: ["sleep", "3600"]